version: '3.8'
services:
  front:
    build: ./public
    ports:
      - 3000:3000
  api:
    build: ./server
    ports:
      - "5001:5001"
    depends_on:
      - db

  db:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
